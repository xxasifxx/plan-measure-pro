# Snapshot · 2026-03-27 — GPS Calibration & Label Tool

## What Existed
- Invitation-based RBAC, multi-user annotation, Supabase persistence, mobile touch.
- Product branded **TakeoffPro**.

## What Just Changed
1. **GPS calibration infrastructure (sha in migrations `20260327122843`):**
   - `geo_calibrations` table: `control_points JSONB`, `transform_matrix`, `estimated_error_ft`.
   - GPS position dot + trace polyline rendered on the overlay canvas.
   - `onGpsPlanTap` intercept for GPS plan-point selection.
   - Calibration UI flow (20 commits across 2026-03-27 to 2026-04-11).

2. **Label tool** (sha `88301626`): leader-line annotations with drag handles, `submitLabel`, dialog prompt.

3. **Draggable endpoint handles** on line/polygon annotations (sha `d46bf056`).

## What Was Abandoned
- Nothing removed — this is an expansion phase.

## Product Thesis at This Moment
> "**TakeoffPro** — a construction quantity take-off platform with GPS georeferencing. Field inspectors can walk a site with a GPS device, tap control points on the plan to establish a coordinate transform, then trace GPS-accurate measurements directly onto the PDF. Measurement tools include lines, polygons, counts, depth prompts, and leader labels."
